{
  "name": "@hovue/website",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Official website for Hovue",
  "scripts": {
    "dev": "astro dev",
    "dev:with-playground": "cd ../.. && pnpm --filter hovue-playground generate && mkdir -p packages/website/public/playground && cp -r playground/.output/public/* packages/website/public/playground/ && cd packages/website && astro dev",
    "predev": "test -d public/playground || (echo '⚠️  Playground not found. Run: pnpm dev:with-playground' || true)",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "astro check",
    "lint": "prettier --check \"src/**/*.{astro,ts,tsx,vue,js,jsx,json,css,md}\"",
    "format": "prettier --write \"src/**/*.{astro,ts,tsx,vue,js,jsx,json,css,md}\"",
    "clean": "rm -rf dist .astro node_modules"
  },
  "dependencies": {
    "@astrojs/vue": "^4.0.0",
    "@hovue/icons": "workspace:*",
    "astro": "^5.16.9",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.2.0",
    "prettier-plugin-astro": "^0.12.0",
    "typescript": "^5.3.0"
  }
}
